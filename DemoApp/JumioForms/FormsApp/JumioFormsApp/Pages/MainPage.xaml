<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:local="clr-namespace:JumioFormsApp"
  x:Class="JumioFormsApp.Pages.MainPage"
  xmlns:vm="clr-namespace:JumioFormsApp.ViewModel">
    <ContentPage.BindingContext>
        <vm:MainPageViewModel />
    </ContentPage.BindingContext>
    <ScrollView>
        <StackLayout Padding="50,100">
            <Button Text="Netverify" Command="{Binding NetverifyCommand}" />
            <Button Text="DocumentVerification" Command="{Binding DocumentVerificationCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>